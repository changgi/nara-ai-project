# NARA-AI CPU 전용 의존성
# GPU 없는 환경 (노트북, RTX 3060 등)에서 실행 가능

# 핵심 프레임워크
fastapi>=0.115.0
uvicorn>=0.34.0
httpx>=0.28.0
pydantic>=2.10.0
pyyaml>=6.0

# 데이터 처리
numpy>=1.26.0
scikit-learn>=1.5.0  # TF-IDF, BM25 대용

# 테스트
pytest>=8.0.0
pytest-asyncio>=0.24.0

# 유틸리티
python-dotenv>=1.0.0

# 참고: torch는 CPU 전용 설치
# pip install torch --index-url https://download.pytorch.org/whl/cpu
# sentence-transformers는 선택 (GPU 있으면 설치)
